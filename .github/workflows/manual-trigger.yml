name: Print Hello World

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:

    - name: Print Hello World
      run: echo "Hello World"
